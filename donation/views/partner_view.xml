<?xml version="1.0"?>
<odoo>
  <data>

    <record id="partner_donation_action" model="ir.actions.act_window">
      <field name="name">Donations</field>
      <field name="res_model">donation.donation</field>
      <field name="view_mode">tree,form,graph</field>
      <field name="context">{'search_default_partner_id': active_id}</field>
    </record>

    <record id="view_partner_form" model="ir.ui.view">
      <field name="name">donation.button.res.partner.form</field>
      <field name="model">res.partner</field>
      <field name="inherit_id" ref="base.view_partner_form" />
      <field name="arch" type="xml">
        <xpath expr="//div[@name='button_box']" position="inside">
          <button class="oe_inline oe_stat_button" type="action" name="%(donation.partner_donation_action)d"
                  attrs="{'invisible': [('customer', '=', False)]}" icon="fa-heart-o">
            <field string="Donations" name="donation_count" widget="statinfo"/>
          </button>
        </xpath>
      </field>
    </record>

    <record id="action_partner_donors_form" model="ir.actions.act_window">
      <field name="name">Donors</field>
      <field name="type">ir.actions.act_window</field>
      <field name="res_model">res.partner</field>
      <field name="view_type">form</field>
      <field name="view_mode">kanban,tree,form</field>
      <field name="context">{}</field>
      <field name="search_view_id" ref="base.view_res_partner_filter"/>
      <field name="domain">[('donation_count', '&gt;', 0)]</field>
    </record>

  </data>
</odoo>
